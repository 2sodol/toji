<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toji.toji.mapper.RegionMapper">

  <insert id="insertBasicInfo" parameterType="BasicInfo">
    <selectKey keyProperty="ilglPrvuInfoSeq" resultType="Long" order="BEFORE">
      SELECT DTJ_MAIN.SEQ_ILGL_PRVU_INFO.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1 (
      ILGL_PRVU_INFO_SEQ,
      GPS_LGTD,
      GPS_LTTD,
      LNDS_UNQ_NO,
      LNDS_LDNO_ADDR,
      HDQR_NM,
      MTNOF_NM,
      ROUTE_CD,
      DRVE_DRCT_CD,
      ROUTE_DSTNC,
      STRC_CLSS_CD,
      OCRN_DATES,
      PRCH_EMNO,
      TRNR_NM,
      RLTR_NM,
      TRNR_ADDR,
      RLTR_ADDR,
      ILGL_PSSRT,
      ILGL_PSSN_SQMS,
      ILGL_PRVU_ACTN_STAT_VAL,
      FSTTM_RGSR_ID,
      FSTTM_RGST_DTTM,
      LSTTM_MODFR_ID,
      LSTTM_ALTR_DTTM,
      USE_YN
    ) VALUES (
      #{ilglPrvuInfoSeq},
      #{gpsLgtd},
      #{gpsLttd},
      #{lndsUnqNo},
      #{lndsLdnoAddr},
      #{hdqrNm},
      #{mtnofNm},
      #{routeCd},
      #{drveDrctCd},
      #{routeDstnc},
      #{strcClssCd},
      #{ocrnDates},
      #{prchEmno},
      #{trnrNm},
      #{rltrNm},
      #{trnrAddr},
      #{rltrAddr},
      #{ilglPssrt},
      #{ilglPssnSqms},
      #{ilglPrvuActnStatVal},
      #{fsttmRgsrId},
      SYSDATE,
      #{lsttmModfrId},
      SYSDATE,
      NVL(#{useYn}, 'Y')
    )
  </insert>

  <insert id="insertActionHistory" parameterType="ActionHistory">
    <selectKey keyProperty="ilglActnCtntSeq" resultType="Long" order="BEFORE">
      SELECT DTJ_MAIN.SEQ_ILGL_ACTN_CTNT.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO DTJ_MAIN.T_LNDS_ILGL_ACTN_CTNT01M1 (
      ILGL_ACTN_CTNT_SEQ,
      ILGL_PRVU_INFO_SEQ,
      ACTN_DTTM,
      ACTN_CTNT,
      FSTTM_RGSR_ID,
      FSTTM_RGST_DTTM,
      LSTTM_MODFR_ID,
      LSTTM_ALTR_DTTM,
      USE_YN
    ) VALUES (
      #{ilglActnCtntSeq},
      #{ilglPrvuInfoSeq},
      #{actnDttm},
      #{actnCtnt},
      #{fsttmRgsrId},
      SYSDATE,
      #{lsttmModfrId},
      SYSDATE,
      NVL(#{useYn}, 'Y')
    )
  </insert>

  <select id="findById" parameterType="Long" resultType="BasicInfo">
    SELECT
      ILGL_PRVU_INFO_SEQ as ilglPrvuInfoSeq,
      GPS_LGTD as gpsLgtd,
      GPS_LTTD as gpsLttd,
      LNDS_UNQ_NO as lndsUnqNo,
      LNDS_LDNO_ADDR as lndsLdnoAddr,
      HDQR_NM as hdqrNm,
      MTNOF_NM as mtnofNm,
      ROUTE_CD as routeCd,
      DRVE_DRCT_CD as drveDrctCd,
      ROUTE_DSTNC as routeDstnc,
      STRC_CLSS_CD as strcClssCd,
      OCRN_DATES as ocrnDates,
      PRCH_EMNO as prchEmno,
      TRNR_NM as trnrNm,
      RLTR_NM as rltrNm,
      TRNR_ADDR as trnrAddr,
      RLTR_ADDR as rltrAddr,
      ILGL_PSSRT as ilglPssrt,
      ILGL_PSSN_SQMS as ilglPssnSqms,
      ILGL_PRVU_ACTN_STAT_VAL as ilglPrvuActnStatVal,
      FSTTM_RGSR_ID as fsttmRgsrId,
      FSTTM_RGST_DTTM as fsttmRgstDttm,
      LSTTM_MODFR_ID as lsttmModfrId,
      LSTTM_ALTR_DTTM as lsttmAltrDttm,
      USE_YN as useYn
    FROM DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1
    WHERE ILGL_PRVU_INFO_SEQ = #{id}
      AND USE_YN = 'Y'
  </select>

  <select id="findAll" resultType="BasicInfo">
    SELECT
      MAX(ILGL_PRVU_INFO_SEQ) as ilglPrvuInfoSeq,
      LNDS_UNQ_NO as lndsUnqNo,
      MAX(LNDS_LDNO_ADDR) as lndsLdnoAddr
    FROM DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1
    WHERE USE_YN = 'Y'
    GROUP BY LNDS_UNQ_NO
    ORDER BY MAX(ILGL_PRVU_INFO_SEQ) DESC
  </select>

  <select id="findAllWithPaging" resultType="BasicInfo">
    SELECT * FROM (
      SELECT
        ROWNUM as rn,
        t.*
      FROM (
        SELECT
          MAX(ILGL_PRVU_INFO_SEQ) as ilglPrvuInfoSeq,
          LNDS_UNQ_NO as lndsUnqNo,
          MAX(LNDS_LDNO_ADDR) as lndsLdnoAddr,
          MAX(GPS_LGTD) as gpsLgtd,
          MAX(GPS_LTTD) as gpsLttd
        FROM DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1
        WHERE USE_YN = 'Y'
        GROUP BY LNDS_UNQ_NO
        ORDER BY MAX(ILGL_PRVU_INFO_SEQ) DESC
      ) t
    ) WHERE rn BETWEEN #{offset} + 1 AND #{offset} + #{limit}
  </select>

  <select id="countAll" resultType="int">
    SELECT COUNT(*)
    FROM (
      SELECT
        LNDS_UNQ_NO
      FROM DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1
      WHERE USE_YN = 'Y'
      GROUP BY LNDS_UNQ_NO
    )
  </select>

  <update id="updateBasicInfo" parameterType="BasicInfo">
    UPDATE DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1
    SET
      GPS_LGTD = #{gpsLgtd},
      GPS_LTTD = #{gpsLttd},
      LNDS_UNQ_NO = #{lndsUnqNo},
      LNDS_LDNO_ADDR = #{lndsLdnoAddr},
      HDQR_NM = #{hdqrNm},
      MTNOF_NM = #{mtnofNm},
      ROUTE_CD = #{routeCd},
      DRVE_DRCT_CD = #{drveDrctCd},
      ROUTE_DSTNC = #{routeDstnc},
      STRC_CLSS_CD = #{strcClssCd},
      OCRN_DATES = #{ocrnDates},
      PRCH_EMNO = #{prchEmno},
      TRNR_NM = #{trnrNm},
      RLTR_NM = #{rltrNm},
      TRNR_ADDR = #{trnrAddr},
      RLTR_ADDR = #{rltrAddr},
      ILGL_PSSRT = #{ilglPssrt},
      ILGL_PSSN_SQMS = #{ilglPssnSqms},
      ILGL_PRVU_ACTN_STAT_VAL = #{ilglPrvuActnStatVal},
      LSTTM_MODFR_ID = #{lsttmModfrId},
      LSTTM_ALTR_DTTM = SYSDATE
    WHERE ILGL_PRVU_INFO_SEQ = #{ilglPrvuInfoSeq}
  </update>

  <delete id="deleteBasicInfo" parameterType="Long">
    UPDATE DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1
    SET USE_YN = 'N',
        LSTTM_ALTR_DTTM = SYSDATE
    WHERE ILGL_PRVU_INFO_SEQ = #{id}
  </delete>

  <select id="findActionHistoriesByBasicInfoId" parameterType="Long" resultType="ActionHistory">
    SELECT
      ILGL_ACTN_CTNT_SEQ as ilglActnCtntSeq,
      ILGL_PRVU_INFO_SEQ as ilglPrvuInfoSeq,
      ACTN_DTTM as actnDttm,
      ACTN_CTNT as actnCtnt,
      FSTTM_RGSR_ID as fsttmRgsrId,
      FSTTM_RGST_DTTM as fsttmRgstDttm,
      LSTTM_MODFR_ID as lsttmModfrId,
      LSTTM_ALTR_DTTM as lsttmAltrDttm,
      USE_YN as useYn
    FROM DTJ_MAIN.T_LNDS_ILGL_ACTN_CTNT01M1
    WHERE ILGL_PRVU_INFO_SEQ = #{basicInfoId}
      AND USE_YN = 'Y'
    ORDER BY ACTN_DTTM DESC
  </select>

  <delete id="deleteActionHistoriesByBasicInfoId" parameterType="Long">
    UPDATE DTJ_MAIN.T_LNDS_ILGL_ACTN_CTNT01M1
    SET USE_YN = 'N',
        LSTTM_ALTR_DTTM = SYSDATE
    WHERE ILGL_PRVU_INFO_SEQ = #{basicInfoId}
  </delete>

  <!-- 첨부파일 관련 쿼리 -->
  
  <!-- 특정 토지의 상세정보 등록일 리스트 조회 -->
  <select id="findDetailDatesByLndsUnqNo" parameterType="String" resultType="map">
    SELECT 
      OCRN_DATES AS ocrnDates,
      PRCH_EMNO AS prchEmno,
      ILGL_PRVU_INFO_SEQ AS ilglPrvuInfoSeq
    FROM DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1
    WHERE LNDS_UNQ_NO = #{lndsUnqNo}
      AND USE_YN = 'Y'
    ORDER BY OCRN_DATES DESC, ILGL_PRVU_INFO_SEQ DESC
  </select>

  <!-- 특정 토지의 사진 등록일 리스트 조회 -->
  <select id="findPhotoDatesByLndsUnqNo" parameterType="String" resultType="map">
    SELECT 
      a.OCRN_DATES AS ocrnDates,
      MAX(p.ILGL_PRVU_INFO_SEQ) AS ilglPrvuInfoSeq,
      MAX(p.PRCH_EMNO) AS prchEmno
    FROM DTJ_MAIN.T_LNDS_ILGL_ATTFL01M1 a
    INNER JOIN DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1 p 
      ON a.ILGL_PRVU_ADDR_SEQ = p.ILGL_PRVU_INFO_SEQ
    WHERE p.LNDS_UNQ_NO = #{lndsUnqNo}
      AND p.USE_YN = 'Y'
      AND a.OCRN_DATES IS NOT NULL
    GROUP BY a.OCRN_DATES
    ORDER BY a.OCRN_DATES DESC
  </select>

  <!-- 특정 SEQ의 상세정보 조회 -->
  <select id="findDetailBySeq" parameterType="Long" resultType="BasicInfo">
    SELECT
      ILGL_PRVU_INFO_SEQ as ilglPrvuInfoSeq,
      GPS_LGTD as gpsLgtd,
      GPS_LTTD as gpsLttd,
      LNDS_UNQ_NO as lndsUnqNo,
      LNDS_LDNO_ADDR as lndsLdnoAddr,
      HDQR_NM as hdqrNm,
      MTNOF_NM as mtnofNm,
      ROUTE_CD as routeCd,
      DRVE_DRCT_CD as drveDrctCd,
      ROUTE_DSTNC as routeDstnc,
      STRC_CLSS_CD as strcClssCd,
      OCRN_DATES as ocrnDates,
      PRCH_EMNO as prchEmno,
      TRNR_NM as trnrNm,
      RLTR_NM as rltrNm,
      TRNR_ADDR as trnrAddr,
      RLTR_ADDR as rltrAddr,
      ILGL_PSSRT as ilglPssrt,
      ILGL_PSSN_SQMS as ilglPssnSqms,
      ILGL_PRVU_ACTN_STAT_VAL as ilglPrvuActnStatVal,
      FSTTM_RGSR_ID as fsttmRgsrId,
      FSTTM_RGST_DTTM as fsttmRgstDttm,
      LSTTM_MODFR_ID as lsttmModfrId,
      LSTTM_ALTR_DTTM as lsttmAltrDttm,
      USE_YN as useYn
    FROM DTJ_MAIN.T_LNDS_ILGL_PRVU_INFO01M1
    WHERE ILGL_PRVU_INFO_SEQ = #{ilglPrvuInfoSeq}
      AND USE_YN = 'Y'
  </select>

  <!-- 특정 SEQ의 사진 리스트 조회 -->
  <select id="findPhotosBySeq" parameterType="Long" resultType="Attachment">
    SELECT
      a.ILGL_ATTFL_SEQ as ilglAttflSeq,
      a.ILGL_PRVU_ADDR_SEQ as ilglPrvuAddrSeq,
      a.ATTFL_NM as attflNm,
      a.ATTFL_PATH as attflPath,
      a.ATTFL_CPCT as attflCpct,
      a.OCRN_DATES as ocrnDates
    FROM DTJ_MAIN.T_LNDS_ILGL_ATTFL01M1 a
    WHERE a.ILGL_PRVU_ADDR_SEQ = #{ilglPrvuInfoSeq}
    ORDER BY a.ILGL_ATTFL_SEQ DESC
  </select>

</mapper>
